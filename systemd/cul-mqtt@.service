[Unit]
Description=CUL MQTT service
After=network-online.target
OnFailure=notify-email@%i.service

[Service]
Type=simple
ExecStart=/opt/cul-mqtt/bin/cul-mqtt -d /dev/%i --tls -c /path/to/certificate -u USERNAME -p PASSWORD -b mqtt.server -v
User=scriptuser

[Install]
WantedBy=multi-user.target
